{
  "name": "purakam-mobile-wrapper",
  "version": "1.0.0",
  "description": "Capacitor wrapper for Purakam local services",
  "main": "index.js",
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
